export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "RazeHost",
  description: "A RazeHost oferece serviços para potencializar seus serviços digitais para tudo que seu projeto precisa e uma equipe que está sempre disposta a te levar ao sucesso.",
  navItems: [
		{
			label: "Inicio",
			href: "/",
		}
	],
};